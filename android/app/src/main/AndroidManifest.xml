<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network access must be explicit for release builds (debug builds get
         it implicitly; a release build without it fails on DNS lookup). -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Background audio playback (audio_service plugin in podcast_core). -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>

    <!-- Android TV (Leanback): touchscreen optional, leanback supported. Without
         these features Play refuses a TV listing and the TV launcher does not
         register the app. The core detects TV mode through the "app/tv_mode"
         channel in MainActivity. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:label="Podcasterium"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Android TV launcher entry. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <!-- App Links: an allowlist of content paths, not the whole host.
                 Android has no "exclude", so a filter without paths would also
                 capture the OAuth return https://podcasterium.com/auth/callback
                 and break web sign-in. Auth routes stay in the browser. Keep in
                 step with the AASA served by the web worker. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="podcasterium.com" />
                <data android:path="/" />
                <data android:path="/channels" />
                <data android:pathPrefix="/v/" />
                <data android:pathPrefix="/m/" />
                <data android:pathPrefix="/c/" />
                <data android:pathPrefix="/p/" />
                <data android:pathPrefix="/handoff" />
            </intent-filter>

            <!-- Custom URL scheme for the Supabase OAuth return
                 (com.podcasterium://auth/callback) and magic links. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.podcasterium" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Impeller off: on low-end TV GPUs its first-frame shader compile adds
             5-10 s to cold start; Skia has precompiled shaders. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- audio_service: foreground media playback service and the
             Bluetooth/headset button receiver. Required for background audio. -->
        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
    </application>
    <queries>
        <!-- Flutter engine ProcessTextPlugin. -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher on Android 11+: declare the intents launchUrl() uses,
             or links to the terms, privacy policy and YouTube do not open. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>
</manifest>
